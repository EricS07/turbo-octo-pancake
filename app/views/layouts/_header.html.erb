<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Ruby-R3", '/', id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <li><%= link_to "Home",   home_path %></li>
        <% else %>
            <li><%= link_to "Home", root_path %></li>
        <% end %>
        <li><%= link_to "Help",   help_path %></li>
            <% if user_signed_in? %>
                <p class="navbar-text"><span class="glyphicon glyphicon-user"></span> Logged in as <%= current_user.username %></p>
                <li><%= link_to "Logout",  destroy_user_session_path, :method => :delete%></li>
            <% else %>
                <li></span><%= link_to "Log in", new_user_session_path %></li>
            <% end %>
      </ul>
    </nav>
  </div>
</header>

