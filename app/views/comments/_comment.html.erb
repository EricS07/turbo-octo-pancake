<p>
  <strong>Comment:</strong>
  <%= comment.body %><br><br>
Posted <%= time_ago_in_words(comment.created_at) %> ago by <%= comment.user ? (link_to comment.user.username) : :null %> <br>
</p>


<p>
  <% if current_user.admin? %><%= link_to 'Destroy Comment', [comment.article, comment],
               method: :delete,
               data: { confirm: 'Are you sure?' } %><% end %>
</p>
<hr>
